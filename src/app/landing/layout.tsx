// Landing pages layout: no Header, no Footer, no WhatsApp button
// Clean conversion-focused layout for Google Ads

export default function LandingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
